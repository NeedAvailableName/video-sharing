{
  "name": "youtube",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ needavailablename/youtube",
    "create-local": "graph create --node http://localhost:8020/ needavailablename/youtube",
    "remove-local": "graph remove --node http://localhost:8020/ needavailablename/youtube",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 needavailablename/youtube",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.50.1",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
